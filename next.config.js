/** @type {import('next').NextConfig} */
// Configuração principal do Next.js para o AdPulse
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['avatars.githubusercontent.com'],
  },
}

module.exports = nextConfig
